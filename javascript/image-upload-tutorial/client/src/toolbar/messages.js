import { defineMessages } from "react-intl";

export const scope = "practice.toolbar.VerticalToolbar";

export default defineMessages({
  buttonTop: {
    id: `${scope}.buttonTop.message`,
    defaultMessage: "Button Top Default Message"
  },
  buttonBottom: {
    id: `${scope}.buttonBottom.message`,
    defaultMessage: "Button Bottom Default Message"
  }
});
